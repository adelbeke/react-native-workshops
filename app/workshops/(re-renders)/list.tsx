import { Text } from "react-native";

export default function Page() {
  return (
    <Text className={"text-xl"}>
      Let's going through some re-renders usecases. You will have to find out
      how we can optimize the re-renders.
    </Text>
  );
}
